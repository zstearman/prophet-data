<h1>All Conferences</h1>
<table>
  <thead>
    <tr>
      <th>id</th>
      <th>Name</th>
      <th>Key</th>
    </tr>
  </thead>
  <tbody>
    <% @conferences.each do |conf| %>
      <tr>
        <td><%= conf.id %></td>
        <td><%= link_to conf.name , conf %></td>
        <td><%= conf.key %></td>
      </tr>
    <% end %>
  </tbody>
</table>